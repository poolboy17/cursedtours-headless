// Dashboard posts disabled - redirect to home
export default function DashboardPostsPage() {
  return null;
}

export async function getServerSideProps() {
  return {
    redirect: {
      destination: '/',
      permanent: true,
    },
  };
}
